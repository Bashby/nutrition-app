// Vendor libs
import * as React from "react";

// Layout
export class Layout extends React.Component<undefined, undefined> {
    render() {
        return (
            <h1>It works?</h1>
        );
    }
}