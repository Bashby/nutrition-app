// Vendor libs
import * as React from "react";
import { Table } from "react-bootstrap";

interface State {
}

interface Props {
}

// Form Participant Details
export class ParticipantDetails extends React.Component<Props, State> {
    constructor(props: Props) {
        super(props);
        this.state = {};
    }

    render() {
        // Render
        return (
            <div>
            </div>
        );
    }
}
